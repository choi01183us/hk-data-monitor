{
  "name": "hk-data-monitor",
  "version": "0.1.0",
  "description": "香港數據監測站 — 畀中學生睇嘅公開數據儀表板（Observable Framework 純靜態網站）",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "observable preview",
    "build": "rm -rf src/.observablehq/cache && observable build",
    "build:offline": "rm -rf src/.observablehq/cache && HKDM_OFFLINE=1 observable build",
    "refresh": "HKDM_REFRESH=1 node scripts/refresh-data.mjs",
    "validate": "node scripts/validate.mjs",
    "clean": "rm -rf dist src/.observablehq/cache"
  },
  "devDependencies": {
    "@observablehq/framework": "1.13.4"
  }
}
